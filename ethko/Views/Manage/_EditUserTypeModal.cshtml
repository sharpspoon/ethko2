@model ethko.UserType
@using (Html.BeginForm("EditSave", "Manage", new { @class = "form-horizontal", role = "form", UserTypeId = Model.UserTypeId }))
{
    <br />
    <h3>User Type: <input name="NewUserType" type="text" placeholder=@Html.DisplayTextFor(m => m.UserTypeName)></h3>
    <br />
    <br />
    <br />
    <br />
    @Html.AntiForgeryToken()
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button> <input type="submit" name="SaveButton" value="Save" class="btn btn-success" />
}